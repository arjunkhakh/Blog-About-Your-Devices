{{#each posts as | post |}}
    <center>
<div class="card">
    <div class="card-body">
    <h3>{{post.title}}</h3>
    <p>{{post.description}}</p>
    <label for="created_at">{{post.created_at}}</label>
     </div>
</div>
    </center>
{{/each}}