<center>
<div class="card">
    <div class="card-body">
    <h3>{{post.title}}</a></h3>
    <p>{{post.description}}</p>
    <label for="created_at">{{post.created_at}}</label>
     </div>
</div>

<div class="card">
    <div class="card-body">
    <p>{{post.comment.comment}}<p>
    <label for="created_at">{{post.comment.created_at}}</label>
     </div>
</div>
    </center>